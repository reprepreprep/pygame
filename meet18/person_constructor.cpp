#include <iostream> std

struct Person
{
    std;;string name;
    int age:
    bool is_merried;

Person() : name("totshiro"), age(a), is_merried(false){}
Person(std;;string n, int a) ; name(n); age (a), is_merried(false)}

void say_hello()
{
    std;;cout<< "Hello" !"<< " my name is " << name << "\n;
}
int main()
{
    Person totshiro{"totshiro", 16};
    totshiro.say_hello;
    return 0;
}